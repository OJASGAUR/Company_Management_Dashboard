import { requireClientPortal } from "@/lib/client-portal"

export default async function ClientProfilePage() {
  const { user, client } = await requireClientPortal()
  return <div className="mx-auto max-w-4xl space-y-6"><div><p className="text-sm font-semibold uppercase tracking-wider text-indigo-600">Client Portal</p><h1 className="mt-1 text-3xl font-bold">Company Profile</h1><p className="mt-2 text-sm text-slate-500">Contact and account information associated with your portal.</p></div><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-center gap-4"><div className="flex h-14 w-14 items-center justify-center rounded-full bg-cyan-100 text-xl font-bold text-cyan-700">{(client?.name || user.name || "C").charAt(0).toUpperCase()}</div><div><h2 className="text-xl font-bold">{client?.name || user.name || "Client"}</h2><p className="text-sm text-slate-500">{client?.company || "Client account"}</p></div></div></section><section className="grid gap-5 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm sm:grid-cols-2"><Item label="Contact email" value={client?.email || user.email} /><Item label="Phone" value={client?.phone || user.phone} /><Item label="Company" value={client?.company} /><Item label="Portal account" value={user.email} /><Item label="Account status" value={user.isActive ? "Active" : "Disabled"} /><Item label="Access" value="Limited Client Portal" /></section></div>
}
function Item({ label, value }: { label: string; value: string | null | undefined }) { return <div><p className="text-xs font-semibold uppercase tracking-wider text-slate-400">{label}</p><p className="mt-1 text-sm text-slate-800">{value || "—"}</p></div> }
