import { getClientPortalData } from "@/lib/client-portal"
import { sendClientMessage } from "../actions"

export default async function ClientMessagesPage() {
  const { client, messages, contacts, user } = await getClientPortalData()
  if (!client) return <div className="rounded-2xl bg-amber-50 p-8 text-amber-900">This client account is not linked to a client record yet.</div>
  return <div className="mx-auto max-w-6xl space-y-7"><div><p className="text-sm font-semibold uppercase tracking-wider text-indigo-600">Client Portal</p><h1 className="mt-1 text-3xl font-bold">Messages</h1><p className="mt-2 text-sm text-slate-500">Communicate directly with your account, operations or finance contacts.</p></div><div className="grid gap-6 lg:grid-cols-[1.5fr_0.5fr]"><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="max-h-[520px] space-y-3 overflow-y-auto pr-1">{messages.map(message => { const mine = message.senderId === user.id; return <div key={message.id} className={`flex ${mine ? "justify-end" : "justify-start"}`}><div className={`max-w-[82%] rounded-2xl px-4 py-3 ${mine ? "bg-slate-950 text-white" : "bg-slate-100 text-slate-800"}`}><p className="text-sm leading-6">{message.content}</p><p className={`mt-1 text-[10px] ${mine ? "text-slate-400" : "text-slate-500"}`}>{message.timestamp.toLocaleString()}</p></div></div>})}{messages.length===0 && <div className="p-10 text-center text-sm text-slate-500">No conversations yet. Send the first message.</div>}</div><form action={sendClientMessage} className="mt-5 border-t border-slate-100 pt-5"><div className="grid gap-3 sm:grid-cols-[220px_1fr_auto]"><select name="receiverId" required className="field"><option value="">Contact team member</option>{contacts.map(contact => <option key={contact.id} value={contact.id}>{contact.name || contact.email} · {contact.role.replace(/_/g, " ")}</option>)}</select><input name="content" required maxLength={4000} placeholder="Write a message..." className="field" /><button className="rounded-lg bg-indigo-600 px-5 py-2.5 text-sm font-semibold text-white hover:bg-indigo-700">Send</button></div></form></section><aside className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-semibold">Your contacts</h2><div className="mt-4 space-y-3">{contacts.map(contact => <div key={contact.id} className="rounded-xl bg-slate-50 p-4"><p className="font-medium text-slate-900">{contact.name || contact.email}</p><p className="mt-1 text-xs text-slate-500">{contact.designation || contact.role.replace(/_/g, " ")}</p></div>)}</div></aside></div></div>
}
